# Unreleased

- Shared dialog and page footers now give buttons equal widths and equal heights, including wrapped labels such as Peaceful Rest benefits.

- Fixed shared card and list buttons inheriting fixed control heights. Item-picker results now grow with their image, name, and source text without overlapping at larger text sizes.

- Removed red destructive-button styling; destructive actions use standard Core buttons.

- Fixed shared field alignment, single-line checkbox labels, content-sized dialog buttons, and resource-list row spacing. Saving a Location keeps its manager open.
- Core now owns D&D5e source filtering (including copied SRD provenance) and an enabled-compendium Item picker; Craftworks and Marketplace share the filter, and Downtime uses the picker.
